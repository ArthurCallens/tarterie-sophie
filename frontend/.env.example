# Public — used by the app in the browser (Vite only exposes VITE_-prefixed vars to client code).
# The anon key is safe to expose client-side: it's constrained by Row Level Security.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
